{
  "name": "doc-scanner",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel --if-present run dev",
    "build": "pnpm -r --if-present run build",
    "test": "pnpm -r --if-present run test",
    "test:integration": "INTEGRATION=1 pnpm -r --if-present run test"
  },
  "engines": {
    "node": "24.17.0",
    "pnpm": "11.8.0"
  },
  "packageManager": "pnpm@11.8.0"
}
